// CommonJS copy of next-intl config to ensure compatibility with environments
// that resolve config using require(). Keeps the same settings as the mjs file.
module.exports = {
  locales: ['en', 'es'],
  defaultLocale: 'en'
}
